<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pizza Realtime Website</title>
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="container mx-auto flex items-center justify-between py-4">
        <div>
            <a href="/"><img src="/images/logo.png" alt="logo"></a>
        </div>

        <div>
            <ul class="flex items-center">
                <li class="ml-6 font-bold"><a href="/">Menu</a></li>
                <li class="ml-6 font-bold"><a href="#">Offers</a></li>

                <% if(user) { %> 
                    <li class="ml-6 font-bold">

                        <form id="logout" action="/logout" method="POST">
                            <a onclick="document.getElementById('logout').submit()" href="#">Logout</a>
                        </form>

                    </li>
                <% }else{ %>     
                    <li class="ml-6 font-bold"><a href="/register">Register</a></li>
                    <li class="ml-6 font-bold"><a href="/login">Login</a></li>
                <% } %>     

                <li class="ml-6 ">
                    <a href="/cart" class="inline-block px-4 py-2 rounded-full flex items-center">

                        <!-- we have to show in "cart symbol" the total quantity of items in cart -->
                        <span class="cartCount text-white font-bold pr-2"><%= session.cart ? session.cart.totalQuantity : ""%> </span>
                        <img src="/images/cart.png">
                    </a>
                </li>
            </ul>
        </div>
    </nav>

